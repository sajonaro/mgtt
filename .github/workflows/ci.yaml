name: ci

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - name: vet
        run: go vet ./...
      - name: test
        run: go test ./...
      - name: build
        run: |
          VERSION=$(cat VERSION)
          go build -ldflags="-X github.com/mgt-tool/mgtt/internal/cli.version=${VERSION}" ./cmd/mgtt
      - name: version
        run: ./mgtt version
